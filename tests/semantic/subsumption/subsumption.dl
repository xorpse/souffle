.decl A() btree_delete
A() <= A() :- true. 
.output A

.decl B(x:number, y:number) btree_delete
B(1,1).
B(1,2).

.decl C(x:number, y:number) 
C(1,1).
C(1,2).

B(x,y1) <= C(x,y2) :- 
   y2 <= y1. 

C(x,y1) <= C(x,y2) :- 
   y2 <= y1. 

.output B, C
